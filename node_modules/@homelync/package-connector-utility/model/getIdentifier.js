"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("..");
function getIdentifier(items, key, validationErrors) {
    if (!items) {
        const message = 'Cannot find the specified identity collection';
        validationErrors.push(message);
        __1.Logger.warn(message);
        return undefined;
    }
    const match = items.filter(x => x.key.toUpperCase() === key.toUpperCase());
    // Exact match expected
    if (match.length !== 1) {
        const message = `Unable to find identifier with key: ${key}`;
        validationErrors.push(message);
        __1.Logger.warn(message);
        return undefined;
    }
    return match[0].value;
}
exports.getIdentifier = getIdentifier;
//# sourceMappingURL=getIdentifier.js.map