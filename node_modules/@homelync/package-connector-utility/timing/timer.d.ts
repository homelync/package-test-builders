import { ITimer } from './iTimer';
export declare class Timer implements ITimer {
    private timings;
    private _duration;
    constructor();
    start(tag: string): void;
    stop(): void;
    clear(): void;
    log(): void;
}
